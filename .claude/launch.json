{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "realm-defense",
      "//": "Node is a portable install and is not on the system PATH (see README), so vite is invoked through its binary directly rather than via npm.",
      "runtimeExecutable": "C:\\Users\\flintz\\nodejs\\node.exe",
      "runtimeArgs": ["node_modules/vite/bin/vite.js"],
      "port": 5173,
      "autoPort": true
    }
  ]
}
